# Task: Fix Frontend Unit and E2E Tests

## Goal
Stabilize the frontend by resolving all failures in unit tests (`.spec.ts`) and end-to-end tests (`src/e2e/critical-journeys.cy.ts`).

## Phases
| Phase | Description | Status |
|-------|-------------|--------|
| 1 | Diagnosis: Run and log all test failures | `completed` |
| 2 | Fix Unit Tests: Resolve `.spec.ts` errors | `completed` |
| 3 | Fix E2E Tests: Resolve Cypress test failures | `todo` |
| 4 | Final Verification: Ensure all tests pass | `todo` |

## Decisions
- [x] Use `planning-with-files` to track progress.

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
